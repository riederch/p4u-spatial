# Bridge ADRs

Bridge ADRs cover server behavior and Bridge-owned architecture: deployment, pairing implementation,
processing, persistence/configuration ownership and repository/service boundaries.

Public protocol semantics belong in `../contracts/`. Headset/client implementation decisions belong
in `../app/`.
