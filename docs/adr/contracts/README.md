# Contracts ADRs

Contracts ADRs define implementation-independent interoperability rules: protocol layering,
identities, authorization boundaries, federation semantics, Spatial/XR/App Sync contracts and
cross-provider invariants.

Implementation details for the Bridge and headset app belong in `../bridge/` and `../app/`.
