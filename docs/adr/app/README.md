# App ADRs

App ADRs cover headset/client architecture: XR runtime integration, vendor adapters, reusable client
features, interaction models and persistent HUD/UI behavior.

Public protocol semantics belong in `../contracts/`. Bridge/server decisions belong in
`../bridge/`.
