# Compatibility: Git Repository Layout

The `raw/model/display` layout is now an optional **Git Repository Backend Profile** rather than a universal protocol storage requirement.

Canonical document:

[Git Repository Backend Profile v0.1](profiles/git-repository.md)

This compatibility file remains so existing links do not break.
